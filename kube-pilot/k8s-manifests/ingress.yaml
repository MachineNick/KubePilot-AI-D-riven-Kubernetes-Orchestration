apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubepilot-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: example.kubepilot.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kubepilot-service
                port:
                  number: 80
